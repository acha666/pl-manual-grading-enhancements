name: Docker deployment E2E

on:
  workflow_call:
    inputs:
      image:
        description: Official PrairieLearn image override (empty uses the pinned digest)
        required: false
        type: string
        default: ""

permissions:
  contents: read

jobs:
  e2e:
    name: ${{ matrix.browser }} against official PrairieLearn Docker deployment
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]
    env:
      PLMGE_BROWSER: ${{ matrix.browser }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install ${{ matrix.browser }} --with-deps
      - name: Start isolated deployment and run browser tests
        env:
          PRAIRIELEARN_IMAGE: ${{ inputs.image }}
        run: npm run test:e2e
      - name: Upload deployment identity, logs, and browser diagnostics
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: prairielearn-e2e-${{ matrix.browser }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            test-results/
            playwright-report/
          retention-days: 14
          if-no-files-found: warn
