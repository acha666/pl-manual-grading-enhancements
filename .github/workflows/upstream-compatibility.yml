name: PrairieLearn latest upstream compatibility

on:
  workflow_dispatch:
  schedule:
    - cron: "17 9 * * 1"

permissions:
  contents: read

jobs:
  latest:
    uses: ./.github/workflows/e2e.yml
    with:
      image: prairielearn/prairielearn:latest
      # Restore saved state, then let upstream migrate it before testing.
